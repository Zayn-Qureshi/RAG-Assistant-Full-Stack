services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data       # persists SQLite DB + raw documents across restarts
      - ./logs:/app/logs       # persists logs across restarts
    restart: unless-stopped
